import { ProductDashboardSteps } from "@lib/types/product-dashboard";
import React from "react";

export default function ProductDashboard({ setStep }: { setStep: React.Dispatch<React.SetStateAction<ProductDashboardSteps>> }) {
    return <div>ProductDashboard</div>;
}
